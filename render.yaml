services:
  - type: web
    name: worldcup-2026-predictor
    runtime: python
    buildCommand: "pip install -r requirements.txt && python data/ingest.py && python scripts/train.py"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
